name: Zendesk Webform
description: Add a webform handler to create Zendesk tickets from Drupal webform submissions
package: Zendesk

type: module
# core: 8.x

dependencies:
  - webform:webform (>=8.x-5.x)

version: '1.1.0'
core: '8.x'
project: 'zendesk_webform'

configure: zendesk_webform.admin_settings

php: 5.6